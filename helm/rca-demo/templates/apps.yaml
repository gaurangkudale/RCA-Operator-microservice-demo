{{- range .Values.services }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .name }}
  labels:
    app: {{ .name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .name }}
  template:
    metadata:
      labels:
        app: {{ .name }}
    spec:
      containers:
        - name: {{ .name }}
          image: "{{ $.Values.image.repository }}:{{ $.Values.image.tag }}"
          imagePullPolicy: {{ $.Values.image.pullPolicy }}
          args: ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8080"]
          ports:
            - containerPort: 8080
          env:
            - name: SERVICE_NAME
              value: {{ .name | quote }}
            - name: DATABASE_URL
              value: "postgres://postgres:password@{{ $.Release.Name }}-postgresql:5432/rca_demo"
            # RCA scenarios and rules in this demo focus on spans/logs. Disable
            # metrics export to avoid periodic OTLP HTTP 404 noise from
            # auto-instrumentation when no metrics ingest endpoint is present.
            - name: OTEL_METRICS_EXPORTER
              value: "none"
            {{- range .env }}
            - name: {{ .name }}
              value: {{ .value | quote }}
            {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
spec:
  selector:
    app: {{ .name }}
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
{{- end }}
